class ButtonInfo {
    id: number;
    title: string;

    constructor(buttonText: string, buttonId: number) {
        this.title = buttonText;
        this.id = buttonId;
      }
}

  export default ButtonInfo;